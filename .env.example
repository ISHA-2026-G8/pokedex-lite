
REACT_APP_GITHUB_CLIENT_ID=Ov23liwiJMzcqvymflHA

# ── Optional: backend token exchange endpoint ─────────────────────────────────
# GitHub requires a server-side step to exchange the code for an access_token
# (because the client_secret must stay private). Point this at your backend:
# REACT_APP_AUTH_BACKEND_URL=http://localhost:4000
